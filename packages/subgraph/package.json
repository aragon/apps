{
  "name": "@aragon/zaragoza-subgraph",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ sarkawttest",
    "create-local": "graph create --node http://localhost:8020/ sarkawttest",
    "remove-local": "graph remove --node http://localhost:8020/ sarkawttest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sarkawttest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.24.0",
    "@graphprotocol/graph-ts": "0.24.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.2.0"
  }
}
